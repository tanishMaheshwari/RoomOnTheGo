<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RoomOnTheGo</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "poppins", sans-serif;
        }

        body {
            background-color: #ccbd98;
            padding-top: 60px;
            /* Space for fixed header */
        }

        header {
            background-color: rgba(148, 135, 105, 0.6);
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            padding: 20px 40px;
            position: fixed;
            top: 0;
            width: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
        }

        .logo img {
            width: 150px;
        }

        .navigation {
            display: flex;
            list-style: none;
        }

        .navigation a {
            font-size: 1.2rem;
            color: black;
            text-decoration: none;
            font-weight: 450;
            margin-left: 40px;
            position: relative;
            transition: color 0.3s;
        }

        .navigation a::after {
            content: "";
            position: absolute;
            left: 0;
            bottom: -6px;
            width: 100%;
            height: 3px;
            background: black;
            border-radius: 5px;
            transform: scaleX(0);
            transition: transform 0.5s;
        }

        .navigation a:hover::after {
            transform-origin: left;
            transform: scaleX(1);
        }

        img {
            width: 100%;
            border-radius: 8px;
            transition: transform 0.3s;
        }

        img:hover {
            transform: scale(1.05);
        }

        button {
            background-color: #948769;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #7a674e;
        }

        .listing-image {
            border: 5px solid #948769;
            border-radius: 16px;
        }
    </style>
</head>

<body>
    <header>
        <div class="logo">
            <img src="logo-3.png" alt="Logo">
        </div>
        <nav>
            <ul class="navigation">
                <li><a href="/">Home</a></li>
                <li><a href="/listings">All Listings</a></li>
                <li><a href="/listings/new">New Listing</a></li>
            </ul>
        </nav>
    </header>
    <h3>Listing details:</h3>
    <ul>
        <li><strong>Image:</strong>
            <img src="<%= listing.image.url %>" alt="Listing image" class="listing-image" />
        </li>
        <li><strong>Title:</strong>
            <%= listing.title %>
        </li>
        <li><strong>Description:</strong>
            <%= listing.description %>
        </li>
        <li><strong>Price:</strong> &#8377;<%= listing.price.toLocaleString("en-IN") %>
        </li>
        <li><strong>Location:</strong>
            <%= listing.location %>
        </li>
        <li><strong>Country:</strong>
            <%= listing.country %>
        </li>
    </ul>
    <a href="/listings/<%= listing._id %>/edit">Edit the listing</a>
    <form action="/listings/<%= listing._id %>?_method=DELETE" method="POST">
        <button>Delete this listing</button>
    </form>
</body>

</html>