<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
        rel="stylesheet">
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RoomOnTheGo</title>
    <style>
        .material-symbols-outlined {
            font-variation-settings:
                'FILL' 0,
                'wght' 400,
                'GRAD' 0,
                'opsz' 24
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Roboto";
            font-size: large;
        }

        body {
            background-color: #f4e8d8;
            /* Lighter and more vibrant tone */
            padding-top: 80px;
            color: #3a3a3c;
        }

        header {
            background-color: rgba(0, 0, 0, 0.2);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 15px 50px;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
        }

        .logo img {
            width: 350px;
        }

        .navigation {
            display: flex;
            list-style: none;
        }

        .navigation a {
            font-size: 1.2rem;
            color: black;
            text-decoration: none;
            font-weight: 450;
            margin-left: 40px;
            position: relative;
            transition: color 0.3s;
        }

        .navigation a::after {
            content: "";
            position: absolute;
            left: 0;
            bottom: -6px;
            width: 100%;
            height: 3px;
            background: black;
            border-radius: 5px;
            transform: scaleX(0);
            transition: transform 0.5s;
        }

        .navigation a:hover::after {
            transform-origin: left;
            transform: scaleX(1);
        }

        img {
            max-width: 100%;
            height: auto;
            border-radius: 10px;
        }

        .listing-image {
            max-width: 600px;
            /* Slightly larger image */
            display: block;
            margin: 40px auto;
            /* Increased margin for centering */
        }

        ul {
            list-style-type: none;
            padding: 20px;
            text-align: center;
            /* Center-aligned text */
        }

        li {
            margin-bottom: 10px;
        }

        button {
            background-color: #7a674e;
            /* Matched to background scheme */
            color: #fff;
            border: none;
            padding: 12px 24px;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.3s;
        }

        button:hover {
            background-color: #946f51;
            /* Slightly lighter for hover effect */
            transform: scale(1.1);
        }

        form {
            margin-top: 10px;
            display: flex;
            justify-content: center;
        }
    </style>
</head>

<body>
    <header>
        <div class="logo">
            <img src="logo-3.png" alt="Logo">
        </div>
        <nav>
            <ul class="navigation">
                <li><a href="/">Home</a></li>
                <li><a href="/listings">All Listings</a></li>
                <li><a href="/listings/new">New Listing</a></li>
            </ul>
        </nav>
    </header>
    <ul>
        <img src="<%= listing.image.url %>" alt="Listing Image" class="listing-image" />
        <li><strong>Title:</strong>
            <%= listing.title %>
        </li>
        <li><strong>Description:</strong>
            <%= listing.description %>
        </li>
        <li><strong>Price:</strong> ₹<%= listing.price.toLocaleString("en-IN") %>
        </li>
        <li><strong>Location:</strong>
            <%= listing.location %>
        </li>
        <li><strong>Country:</strong>
            <%= listing.country %>
        </li>
    </ul>
    <p style="text-align: center;"><span class="material-symbols-outlined">
            call
        </span><b>Contact Us: +91 92864756XX </b></p>
    <a href="/listings/<%= listing._id %>/edit" style="display: block; text-align: center;">Edit the listing</a>
    <form action="/listings/<%= listing._id %>?_method=DELETE" method="POST">
        <button>Delete this listing</button>
    </form>


</body>

</html>